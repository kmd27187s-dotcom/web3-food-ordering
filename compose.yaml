services:
  postgres:
    image: postgres:16
    container_name: mealvote-postgres
    environment:
      POSTGRES_DB: mealvote
      POSTGRES_USER: mealvote
      POSTGRES_PASSWORD: mealvote
    ports:
      - "5432:5432"
    volumes:
      - mealvote-postgres:/var/lib/postgresql/data

  backend:
    build: ./backend
    container_name: mealvote-backend
    ports:
      - "8080:8080"
    environment:
      - HTTP_ADDR=:8080
      - DATABASE_URL=postgres://mealvote:mealvote@postgres:5432/mealvote?sslmode=disable
      - DB_AUTOMIGRATE=true
      - RATE_LIMIT_LOGIN_MAX=0
      - RATE_LIMIT_REGISTER_MAX=0
      - CHAIN_ID=11155111
      - RPC_URL=
      - ORDER_CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000
      - MEMBERSHIP_TOKEN_ADDRESS=0x0000000000000000000000000000000000000000
      - PLATFORM_TREASURY_ADDRESS=0x0000000000000000000000000000000000000000
    depends_on:
      - postgres

  frontend:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    container_name: mealvote-frontend
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_BASE=http://localhost:8080
    depends_on:
      - backend

volumes:
  mealvote-postgres:
